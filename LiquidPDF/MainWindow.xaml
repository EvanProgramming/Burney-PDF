<Window x:Class="LiquidPDF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LiquidPDF"
        xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
        mc:Ignorable="d"
        Title="Liquid PDF"
        Height="800"
        Width="1200"
        MinHeight="400"
        MinWidth="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen" SizeChanged="Window_SizeChanged" MouseDoubleClick="Window_MouseDoubleClick" PreviewKeyDown="Window_PreviewKeyDown" KeyDown="OnKeyDown"
        AllowDrop="True" Drop="OnFileDrop" DragOver="OnDragOver">
    <Border x:Name="WindowBorder" CornerRadius="12" Background="#1E1E24" BorderThickness="1" BorderBrush="#2A2A32">
        <Grid>
            <!-- 标题栏 -->
            <Grid Height="52" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 左侧图标 -->
                <Ellipse Grid.Column="0" Width="32" Height="32" Fill="#4A90E2" Margin="16,0,8,0"/>
                
                <!-- 中间标题 -->
                <TextBlock Grid.Column="1" Text="Liquid PDF" FontSize="14" FontWeight="Medium" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                
                <!-- 右侧按钮 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Width="36" Height="28" Margin="0,0,1,0" Background="Transparent" Foreground="#FFFFFF" MouseEnter="WindowButton_MouseEnter" MouseLeave="WindowButton_MouseLeave" Click="MinimizeButton_Click">
                        <Path Data="M0,9 L10,9" Stroke="#FFFFFF" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                    </Button>
                    <Button x:Name="MaximizeButton" Width="36" Height="28" Margin="1,0,1,0" Background="Transparent" Foreground="#FFFFFF" MouseEnter="WindowButton_MouseEnter" MouseLeave="WindowButton_MouseLeave" Click="MaximizeButton_Click">
                        <Path Data="M1,1 L9,1 L9,9 L1,9 Z" Stroke="#FFFFFF" StrokeThickness="2"/>
                    </Button>
                    <Button Width="36" Height="28" Margin="1,0,16,0" Background="Transparent" Foreground="#FFFFFF" MouseEnter="CloseButton_MouseEnter" MouseLeave="WindowButton_MouseLeave" Click="CloseButton_Click">
                        <Path Data="M1,1 L9,9 M9,1 L1,9" Stroke="#FFFFFF" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                    </Button>
                </StackPanel>
            </Grid>
            
            <!-- 内容区域 -->
            <skia:SKElement x:Name="MainCanvas" Margin="0,52,0,0" PaintSurface="MainCanvas_PaintSurface" MouseWheel="OnMouseWheel" MouseDown="OnMouseDown"/>
        </Grid>
    </Border>
</Window>